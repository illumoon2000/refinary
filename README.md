Refinary - Deterministic API Refinement Layer for LLMs

Refinary is a control layer that deterministically refines API responses before execution, enabling LLMs to consume data with fewer tokens and fewer hallucinations.
